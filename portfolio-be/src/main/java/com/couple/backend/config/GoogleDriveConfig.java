package com.couple.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GoogleDriveConfig {
    // Google Drive config will be handled in GoogleDriveService
    // Using API key-based authentication for public folder access
}
